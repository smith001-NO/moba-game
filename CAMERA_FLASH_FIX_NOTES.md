# Camera Flicker Hotfix

本包只做闪烁修复，不重新改操作逻辑。

## 修复点

- Camera3D 虽然仍在英雄场景中实例化，但脚本现在在 `_enter_tree()` 和 `_ready()` 中强制 `top_level = true`。
- 这样相机不再继承英雄父节点的旋转。英雄转向时，不会先把相机带着转一帧、再被脚本拉回，从源头避免画面闪烁/抖动。
- 相机更新从 `_process()` 改到 `_physics_process()`，与 CharacterBody3D 移动节奏一致，减少移动跟随 jitter。
- 默认关闭屏幕边缘移动镜头，避免鼠标停在边缘时镜头持续平移，被误认为闪烁。
- 保留上一版的移动方式：WASD 移动英雄，方向键移动镜头，鼠标点击地面移动，点击敌人攻击。

## 操作

- WASD：移动英雄
- 左/右键地面：移动英雄
- 左/右键敌人：追击普攻
- 方向键：移动镜头
- 滚轮：缩放
- Space：镜头回到英雄
- Y：锁定/解锁镜头跟随
- Q/E/R：技能
- F：锁定最近敌人
- P：暂停
